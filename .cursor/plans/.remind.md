Prepare this repo’s cloud environment for UI8Kit workflows: install Bun (v1.3+), ensure bun is on PATH, run bun install for workspace dependencies, and verify bun run typecheck succeeds in workspace