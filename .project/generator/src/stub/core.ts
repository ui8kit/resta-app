/**
 * Stub for @ui8kit/core when type-checking generator.
 * At runtime, core-component-scanner dynamically imports the real @ui8kit/core
 * (from engine or packages/core) and falls back to a manual list if unavailable.
 */
export const __stub = true;
