import { UtensilsCrossed, ChefHat, FileText, Percent } from 'lucide-react';

export const NAV_ICONS: Record<string, typeof UtensilsCrossed> = {
  menu: UtensilsCrossed,
  recipes: ChefHat,
  blog: FileText,
  promotions: Percent,
};
